var user = {name : "Ashish", age : 18};

console.log("My name is Ashish.");

console.log(user.name);

// tsc filename.ts

// run this typescript file by writing .... tsc filename.ts ('tsc 1_intro.ts' in this case) and it automatically produces the another .js file with same filename and the equivalent code in javaScript.