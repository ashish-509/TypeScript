// Syntax : 
//  let variableName : type = value

let greetings : string = "Hello Ashish !";
console.log(greetings);

// ran the program with ( tsc 2_variables.ts ) in the terminal which created equivalent .js file as well.

export {} // temporarily removes the error.

